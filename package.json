{
  "name": "buildit",
  "version": "0.1.0",
  "description": "grbl G-code optimizer and honest cycle-time estimator",
  "type": "module",
  "bin": {
    "buildit": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "MACHINES.md"
  ],
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "node --test tests/*.test.ts",
    "start": "node dist/index.js"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^5.9.3"
  }
}
